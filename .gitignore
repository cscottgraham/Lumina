# Xcode / Swift
.DS_Store
build/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcuserstate
*.moved-aside
*.pbxuser
!default.pbxuser

# XcodeGen generates the project; don't commit it
Lumina.xcodeproj/

# Swift Package Manager
.swiftpm/
.build/
Packages/
Package.resolved

# Secrets — API keys live in the iOS Keychain, never in the repo
*.xcconfig.local
Secrets.plist
.env

# Code-signing material — lives in C:\Users\drcsg\Projects\Lumina-signing and
# GitHub Actions secrets; must never be committed even if copied here.
*.key
*.csr
*.cer
*.pem
*.p12
*.mobileprovision
AuthKey_*.p8

# Fastlane / misc
fastlane/report.xml
fastlane/screenshots
*.ipa
*.dSYM.zip
